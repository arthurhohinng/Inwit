For the Add GPS option to view location of restaurant on map sub-task, I (Arthur) was planning 
on using the google maps API to show the location on the map. But the API requires an API key from the 
INWIT owner so we just have to wait for that. Just like IN-39.